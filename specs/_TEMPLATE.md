# Spec: <use case name>

> One spec per use case. Tests in `tests/` MUST mirror the acceptance
> criteria below, one `it()` per criterion, in order. If you change a
> criterion, change the test in the same commit. Agents: write or update
> this file BEFORE writing implementation code.

## Intent
<one sentence: what user goal this serves>

## Input
<typed shape>

## Acceptance criteria
1. GIVEN ... WHEN ... THEN ...
2. ...

## Out of scope
<explicitly not handled here>
